using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArcherRun : TitleRunner {
	
	// Update is called once per frame
	void Start () {
        anim.SetBool("Moving", true);
	}
}
